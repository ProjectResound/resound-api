default: &default
  adapter: postgresql
  pool: 5
  timeout: 5000
  encoding: utf8
  locale: en_US.UTF-8
  host: db
  port: 5432

development:
  <<: *default
  database: resound_dev
  username: louise.yang
  host: localhost

test:
  <<: *default
  database: resound_test
  username: louise.yang
  host: localhost

production:
  <<: *default
  database: resound
  username: postgres

